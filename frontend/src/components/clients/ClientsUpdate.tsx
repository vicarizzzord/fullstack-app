import { useState } from "react";
import { api } from "../../libs/axios";
import { Clients } from "../../types/clients";

const ClientsUpdate = () => {
  const [fields, setFields] = useState<Clients>();
  const [client, setClient] = useState<Clients>();



  return;
};

export { ClientsUpdate };
